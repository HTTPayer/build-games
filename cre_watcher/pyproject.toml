[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "cre-watcher"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "eth-account>=0.13.7",
    "python-dotenv>=1.2.2",
    "web3>=7.14.1",
]

[tool.uv]
package = true
